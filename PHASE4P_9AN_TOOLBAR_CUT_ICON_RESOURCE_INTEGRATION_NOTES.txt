HOTFIX134 / Phase 4P.9AN — Toolbar Cut Icon Resource Integration

Summary
- Integrated the supplied Toolbar Cut icon into the application resources.
- Replaced the toolbar Cut button's font glyph with a packaged image resource.

Resource placement
- Added original source asset: src/main/resources/icons/ContextMenu.Cut.svg
- Added packaged toolbar render asset: src/main/resources/icons/toolbar.cut.png

FXML update
- MainLayout.fxml now renders the Cut toolbar button graphic with an ImageView that loads /icons/toolbar.cut.png.
- The toolbar button keeps the existing tooltip, accessibility text, and icon-only command-bar behavior.

Packaging sync
- Mirrored the updated resource assets and MainLayout.fxml under target/classes so the full-project archive remains internally consistent.

Build note
- Maven was not available in this container, so this package is a source-level resource integration based on the HOTFIX133 baseline.
