FileExplorer Phase 4I — Performance polish / first-paint + interaction latency (HOTFIX24)

Baseline:
- FileExplorer_PHASE4H_1_TABS_HOME_BEHAVIOR_HOTFIX23

Changes in this phase:
- Deferred non-critical CSS layers (side pane, context menu, Home/tabs) to idle startup work.
- Debounced command-bar compaction updates during window resize to reduce CSS churn.
- Debounced preview thumbnail work while keeping details/status text immediate.
- Coalesced expensive TableView refresh calls triggered by metadata backfill and directory switches.

Files touched:
- src/main/java/com/fileexplorer/app/MainApp.java
- src/main/java/com/fileexplorer/controller/MainController.java

Notes:
- This phase is intentionally code-light and compile-safe: it targets latency and layout churn without changing FXML structure.
- No resource path changes were introduced.
