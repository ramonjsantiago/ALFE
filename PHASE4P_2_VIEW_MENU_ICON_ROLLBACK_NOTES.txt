HOTFIX86 / Phase 4P.2 — View Menu Icon Rollback

Purpose
- back out the HOTFIX85 View menu vector icon experiment because the result made the View menu look worse in practice
- restore the prior View menu presentation and spacing from the HOTFIX84 baseline

Changes
- removed the HOTFIX85 vector-icon wiring from the View menu rows in MainController
- restored the previous app-main.css styling so the View menu no longer shows the generated icon blocks
- removed the ViewMenuIcons helper class introduced in HOTFIX85

Result
- the View menu returns to the pre-HOTFIX85 appearance
- no thumbnail pipeline changes were altered as part of this rollback
