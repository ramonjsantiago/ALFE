HOTFIX113 / Phase 4P.9S — Command Bar Vector Icon Restore

Summary
- Replaced the New / Sort / View command-bar leading glyph dependency with dedicated vector icons.
- Kept the structured icon + label + chevron layout from HOTFIX112 intact.
- Avoided the Segoe glyph fallback path that was rendering as ellipsis (...).

Implementation
- MainController now builds purpose-specific vector icons for New, Sort, and View using SVGPath nodes.
- Added command-bar CSS tokens for vector icon stroke rendering in dark and light themes.
- The chevron slot and overall command-bar menu-button structure remain unchanged from HOTFIX112.

Expected result
- New, Sort, and View each show a stable visible icon instead of ellipsis.
- Down chevrons continue to render in their dedicated trailing slot.
- Full-button flyout hit targets and existing menus continue to behave the same way.
