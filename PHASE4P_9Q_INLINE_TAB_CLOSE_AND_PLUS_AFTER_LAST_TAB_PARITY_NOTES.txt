HOTFIX111 / Phase 4P.9Q — Inline Tab Close and Plus-After-Last-Tab Parity

Changes in this phase
- moved the tab close affordance from a standalone strip-level X button into each visible tab surface so the X now sits immediately after the tab label
- moved the + button so it renders directly after the last visible tab instead of being separated by a flexible spacer
- kept the standalone strip close button hidden/unmanaged as a compatibility fallback while the visible close affordance lives inside the tab content
- preserved the existing single-tab safety guard so closing the final visible tab still leaves one explorer tab visible

Files touched
- src/main/java/com/fileexplorer/controller/MainController.java
- src/main/resources/com/fileexplorer/ui/layout/MainLayout.fxml
- src/main/resources/com/fileexplorer/ui/css/explorer-override-everything.css
