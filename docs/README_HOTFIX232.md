# HOTFIX232 / Phase 4P.9EH — Tree Leaf Indent Final Adjustment

This hotfix continues from the H231 baseline and applies the requested follow-up alignment tweak:

1. Navigation-tree leaf disclosure padding is reduced by 2 additional pixels so non-directory items sit 2 pixels closer to the left while directory row alignment remains unchanged.
2. Both the shared base tree stylesheet and the navigation-pane parity override are kept in sync so the updated leaf indent is applied consistently wherever the Explorer tree styling is used.

Updated files are listed in `CHANGED_FILES.txt`.
