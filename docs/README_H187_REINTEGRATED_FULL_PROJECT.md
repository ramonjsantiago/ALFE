# HOTFIX187 Reintegrated Full Project

This package restores the larger fully populated project footprint by using the HOTFIX186 compile-fix archive as the packaging baseline and overlaying all later HOTFIX187 updates:

- idle-budget menu/view prewarm changes
- application and tray icon update
- dedicated default icons for INI, MSI, PDF, TXT/TEXT

The goal is to preserve the full mirrored `target/classes` payload expected by the user workflow while keeping the latest code and resource changes.
