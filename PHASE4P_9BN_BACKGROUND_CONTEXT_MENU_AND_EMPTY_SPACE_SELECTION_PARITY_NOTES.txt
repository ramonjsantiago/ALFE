HOTFIX160 / Phase 4P.9BN — Background Context Menu and Empty-Space Selection Parity

Summary
- Split empty-space right-click handling onto a dedicated background context menu for file views.
- Added Explorer-style background commands for View, Sort by, Undo, Paste, New, Select all, Refresh, and Properties.
- Kept Group by present as a disabled placeholder path for future routing.
- Ensured item menus, background menus, and header menus dismiss each other.
- Hid transient Explorer menus when the active view changes or when folder navigation begins.
- Refreshed selection presentation after Select all / Select none / Invert selection so icon views and Details repaint immediately.

Implementation notes
- The background menu now tracks the current ViewMode and SortKey through radio-menu state when it opens.
- Empty-space background menu invocation is anchored to the actual active surface node when possible rather than always falling back to the table host.
- Paste shortcut is present as a placeholder row and remains disabled in this phase; actual shortcut materialization is deferred to a later shell-command phase.
