HOTFIX37 / Phase 4K.2D — Details Hover Stabilization + Correct Per-Item Tooltips

Changes:
- moved Details hover ownership from table-level mouse-move tracking to per-row hover state
- kept Details hover highlight on the TableRow only, reducing row-wide pseudo-class churn while moving across columns
- deferred metadata-driven table refreshes while a Details row is hovered, then flushed once hover exits
- added correct per-row Details tooltips that rebuild from the hovered row item at show-time
- added correct per-item icon/tile/list/content tooltips that rebuild from the hovered path at show-time

Intent:
- stop lingering Details/Table hover flicker caused by refresh churn during metadata fill
- prevent recycled tooltip state from showing metadata for the first item instead of the hovered item
