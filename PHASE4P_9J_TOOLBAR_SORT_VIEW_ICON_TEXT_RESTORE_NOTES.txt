Phase 4P.9J / HOTFIX103

Objective
- Restore the toolbar Sort and View entries to the Windows-style icon+label presentation while keeping the HOTFIX99 icon-only treatment for the other leading toolbar commands.

Implementation
- Stopped applying the icon-only command-bar helper to sortMenuButton and viewMenuButton.
- Added a labeled command-bar helper that preserves tooltip/accessibility wiring, removes any icon-only style class, and keeps icon/text left aligned with a 6px gap.
- Added a targeted CSS rule for .command-bar-icon-text so Sort and View return to computed-width menu buttons with normal left padding.

Result
- Sort and View now render as icon, name on the toolbar.
- Their existing flyout hierarchy and actions remain unchanged.
